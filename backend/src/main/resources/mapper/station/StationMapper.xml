<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.barotago.backend.station.dao.StationDAO">
	<!-- 역 기본 정보 단건 조회 -->
	<select id="findStationDetailById" resultType="StationDetailResponseDTO">
		select
		    station_id,
		    station_name,
		    address,
		    lat,
		    lng,
		    telno
		from station
		where station_id = #{stationId};
	</select>
	<!-- 역 편의시설 목록 조회 -->
	<select id="findFacilitiesByStationId" resultType="StationFacilityResponseDTO">
		select
		    f.facility_name,
		    f.icon
		from station_facility sf
		join facility f
		  	on sf.facility_id = f.facility_id
		where sf.station_id = #{stationId};
	</select>
</mapper>